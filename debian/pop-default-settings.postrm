#!/bin/sh

set -e

case "$1" in
    remove)
        dpkg-divert --package pop-default-settings --remove --rename \
                    --divert /etc/os-release.diverted /etc/os-release
        ;;
esac

#DEBHELPER#

exit 0
